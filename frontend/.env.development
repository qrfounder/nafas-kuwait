# Local `npm run dev` uses the live API so checkout and analytics work without running Docker.
# Orders from localhost will hit production — use test names/numbers only.
VITE_API_URL=https://api.naffas.shop
