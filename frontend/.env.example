# Local dev: copy to .env.development — use https://api.naffas.shop if Docker API is not running
VITE_API_URL=http://localhost:8000
VITE_META_PIXEL_ID=
VITE_TIKTOK_PIXEL_ID=
VITE_SNAP_PIXEL_ID=
# Note: STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET are backend-only — never put them in VITE_*.
